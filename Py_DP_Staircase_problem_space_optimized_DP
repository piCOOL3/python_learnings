def valid_ways_DP_space_optimized(destination):
    "this objective function will calculate the valid ways to reach the destination from the current position"

    """

    DP = [0] * (destination + 1 )
    DP[destination] = 1
    DP[destination - 1] = 1
    -------this space can still be optimized as we need to store only next and nex_next values.

    """
    next = 1;
    next_next = 1;

    for state in range((destination - 2) , -1 ,-1 ):
        current = next + next_next;
        next_next = next;
        next = current;

    return current;
