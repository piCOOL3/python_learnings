"add the first n numbers using dynamic programming. "




def find_sum_using_DP_using_recursion_tail_recursion(sum , upperlimit):
    " using recursion ,find the sum value"
    " f(n) = f(n-1) + n : n > 1 , f(1) = 1  "
    if (upperlimit <= 1):
        sum = sum + upperlimit; 
        return sum ;
    else:
        sum = sum + upperlimit ;
        sum = find_sum_using_DP_using_recursion_tail_recursion(sum , upperlimit - 1 );
        "tail recursion : first calculation --> recursion call"
        return sum ;


sum = 0;
upperlimit = int(input("enter the upper limit of the range : "));
sum = find_sum_using_DP_using_recursion_tail_recursion(sum , upperlimit);
print("the sum of natural numbers  till upperlimit : "  + str(upperlimit) + " is  : " + str(sum) , end = " "  );



